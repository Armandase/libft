#include <stdio.h>
#include <string.h>
int main(void)
{   
    char str[] = "test";
    ft_memset(str, '-', 3);
    char str0[] = "test";
    memset(str0, '-', 3);
    printf("us : %s\n mine : %s", str0, str);
}   
~   
